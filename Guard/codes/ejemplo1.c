
#include <stdio.h>
#include <cs50.h>

int suma (int a, int b);

int main ()
{
    int a = get_int("Ingrese un numero: ");
    int b = get_int("Ingrese un numero: ");

    switch (x)
    {

    }
    int resultado = suma(b, a);
    printf ("%d \n",resultado);
    return 0;
}

int suma (int a, int b)
{
    return (a+b);
}
int resta (int a, int b)
{
    return (a-b);
}

// Hacer calculadora con case, suma, resta, mltiplicaion y division